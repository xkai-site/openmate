# AINative OS 架构总览

## 1. 目标

在应用层构建一个 AINative 操作系统：

1. 复用文件系统范式组织 `Topic` 与 `Node`。
2. 以 Agent 作为可调度的执行资源；若需要不同工作风格，优先在能力层通过 Prompt / Plan 组装，而不是在资源层固化角色。
3. 把调度、执行、能力注入、模型调用解耦，支持并行开发与演进。

## 2. 核心设计决策

1. 任务中心而非 CPU 中心：系统重点控制任务，不强绑定 Agent 与队列。
2. 双队列调度：一级按主题，二级按叶子 Node 优先级。
3. 交互驱动优先：一级优先级由用户交互热度决定，采用多级反馈思想。
4. 可插拔能力层：上下文、工具、skill、存储抽象化，避免过早绑定实现。

## 3. 模块拆分

1. 虚拟文件系统：`Node/Topic` 抽象、树结构组织、叶子节点可操作约束。
2. 调度队列：双队列、反馈调度、抢占与并发分配。
3. Agent 池：资源分配、限流、健康管理、原始计量。
4. Agent 能力：上下文注入、工具注入、skill 注入、对话存储。

## 4. 跨模块对接约定（2026-04-09）

### 4.1 Agent池对接面

1. 用户侧只维护 `model.json`。
2. 调度/能力模块通过以下命令与池交互：`get / done / cap / tickets / usage / sync`。
3. 核心写路径为 `get` 与 `done`，其余为查询或配置同步。

### 4.2 状态与追踪约束

1. 每次 `get` 生成新的 `ticket_id`。
2. `done` 统一按 `ticket_id` 回收。
3. 追踪主链路固定为：`node_id -> ticket_id -> api_id`。

### 4.3 并发与一致性约束

1. Agent池运行态使用 SQLite（WAL）。
2. `get/done` 以事务（`BEGIN IMMEDIATE`）执行，避免并发超配。
3. 同时受 `max_concurrent`（API级）和 `global_max_concurrent`（全局级）约束。

## 5. 跨模块已知问题（2026-04-11）

1. 本次在 Windows 环境执行 `python -m unittest discover -s tests` 时，`openmate_pool` 相关测试会在 `TemporaryDirectory` 清理阶段报 `pool_state.db` 被占用。
2. 初步定位在 [store.py](D:/XuKai/Project/vos/openmate_pool/store.py) 的 SQLite 连接清理路径，现象是数据库文件句柄未及时释放，导致临时目录删除失败。
3. 该问题会影响全量测试通过率，但不属于当前 VFS 初始化引入的问题；后续由 Agent池方向统一修复更合适。

## 6. VFS 技术栈变更（2026-04-11）

1. 虚拟文件系统模块已由 Python 实现切换为 Go 独立 CLI。
2. 当前边界仍然是 `vos` 命令 + JSON 输入输出，不引入 RPC 或服务化接口。
3. 存储层暂时仍为 `.vos_state.json`，后续如需升级 SQLite，再单独评估迁移。

## 7. Go 迁移协作状态（2026-04-11）

1. 按当前协作进展，`vos` 与 `pool` 都已从 Python 转向 Go。
2. 当前仓库快照中已落地的是 `vos`，`pool` 以其对应分支为准。
3. 为降低后续合并成本，Go 代码统一按单仓库、单 `go.mod`、多 `cmd/*`、多 `internal/*` 结构推进。
4. 具体约定见 [Go仓库约定.md](D:/XuKai/Project/vos/architecture/sharedInfo/Go仓库约定.md)。
5. 多 worktree 的实际分支合并流程见 [Worktree合并流程.md](D:/XuKai/Project/vos/architecture/sharedInfo/Worktree合并流程.md)。
